# Django Expert

Broad Django backend guidance covering models, ORM usage, views, Django REST
Framework, testing, security, performance, and production practices.

The portable skill and its references are under `skills/django-expert/`. Host
manifests and this package-local projection are generated from the repository
catalog.

See the shared [installation and lifecycle guide](../../docs/installation.md).
