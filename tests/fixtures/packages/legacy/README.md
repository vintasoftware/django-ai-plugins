# Legacy package fixture

Legacy flat `skills/SKILL.md` packages are assembled dynamically in temporary
directories by `tests/test_adapter_generation.py`. This directory documents the
fixture contract without keeping a second discoverable skill tree in the
repository.
