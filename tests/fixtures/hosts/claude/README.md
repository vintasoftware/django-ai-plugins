# Claude fixture

Tests materialize each generated package into a temporary Claude-style cache and
assert that exactly one package-local skill or agent is discoverable. No real
Claude configuration or cache is used.
