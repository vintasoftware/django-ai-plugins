# Codex fixture

Tests materialize each generated package into a temporary Codex-style cache and
resolve the `skills` path from its `.codex-plugin/plugin.json`. No real Codex
configuration or cache is used.
